
describe('AssociationService', function() {
    it ('should exist', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be able to assign in a "single" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be able to unassign in a "single" association', function(done) {
        should.exist(AssociationService);
        done();
    });     
    it ('should be reject an invalid parent when assigning in a "single" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid child when assigning in a "single" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid parent when unassigning in a "single" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid child when unassigning in a "single" association', function(done) {
        should.exist(AssociationService);
        done();
    }); 
    it ('should be able to assign in a "many" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be able to unassign in a "many" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid parent when assigning in a "many" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid children when assigning in a "many" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid parent when unassigning in a "many" association', function(done) {
        should.exist(AssociationService);
        done();
    });
    it ('should be reject an invalid children when unassigning in a "many" association', function(done) {
        should.exist(AssociationService);
        done();
    });                       
});